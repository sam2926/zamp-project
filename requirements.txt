fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
python-doctr[torch]>=0.10
pymupdf>=1.26
openai>=1.50
anthropic>=0.40          # the UI accepts an Anthropic key too; used only when one is pasted
python-dotenv>=1.0
